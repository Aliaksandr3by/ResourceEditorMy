@using Microsoft.AspNetCore.Mvc.Rendering
@section scripts {
    <script type="text/javascript">
        const urlControlActionDelete = "@Url.Action("Delete", "Home")";
        const urlControlActionUpdate = "@Url.Action("Update", "Home")";
        const urlControlActionDataProtect = "@Url.Action("DataProtect", "Home")";
        const urlControlSwitchLanguage = "@Url.Action("SwitchLanguage", "Home")";
        const urlControlUploadFile = "@Url.Action("UploadFile", "Home")";
        const urlControlGetFile = "@Url.Action("GetFile", "Home")";
        const urlControlSelectCountry = "@Url.Action("SelectCountry", "Home")";

    </script>
}

@{
    ViewBag.Title = "Main Table";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col s9">
        <input type="file" id="FileResource" multiple required accept=".resx">
    </div>
    <div class="col s3">
        <button id="ResourceUploads" class="btn" type="button">Send</button>
    </div>
</div>
<div class="row">
    <div class="col s6 centered" id="CountrySelect">

    </div>
    <div class="col s3 centered">
        <button id="countrySelectRefresh" class="btn" type="button">Refresh</button>
    </div>
    <div class="col s3 centered ">
        <button id="ResourceSave" type="button" class="btn">Download</button>
    </div>
</div>
<div class="row">
    <div class="col s12">
        <div id="rootMainTable">
            <table id="mainTable" class="highlight centered responsive-table">
                <thead>
                    <tr>
                        <th scope="col">Key</th>
                        <th scope="col">Value</th>
                        <th scope="col">Comment</th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody id="mainDataBodyTable"></tbody>
            </table>
        </div>
        <div>
            <button type="button" class="btn btn-warning d-inline w-100" id="addTableRow">+</button>
        </div>
    </div>
</div>
