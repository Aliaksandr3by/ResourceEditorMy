@model ResourceEditor.Models.LangName

<script>
    let urlControlDeleteMethod = "@Url.Action("DeleteLineResourceControl", "Home")";
    let urlControlEditMethod = "@Url.Action("MainTableDataResource", "Home")";
    let urlJsonResolverMethod = "@Url.Action("JsonResolver", "Home")";
    
</script>

@{
    ViewBag.Title = "CreateMainTable";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>CreateMainTable</h2>



@{
    <div class="mainTableResource">
        <form method="post" action="@Url.Action("MainTableResource", "Home")">
            @{
                <p>
                    <select id="countrySelectKendo" name="Id">
                        <option disabled selected>Select language</option>
                        @foreach (var item in ResourceEditor.Managers.XmlManager.GetLanguages())
                        {
                            <option value="@item.Id">@item.Value</option>
                        }
                    </select>
                </p>
            }
            <div id="example">
                <div id="grid"></div>
            </div>
        </form>
    </div>
}