@model ResourceEditor.Models.LangName

<script>
    let crudServiceBaseUrlRead = "@Url.Action("Read", "TableEditResource")"; //read
    let crudServiceBaseUrlupdate = "@Url.Action("Edit", "TableEditResource")"; //update
    let crudServiceBaseUrlDestroy = "@Url.Action("Delete", "TableEditResource")"; //destroy
    let crudServiceBaseUrlCreate = "@Url.Action("Create", "TableEditResource")"; //create
</script>

@{
    ViewBag.Title = "CreateMainTable";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>CreateMainTable</h2>


<div class="mainTableResource">
    <p>
        <select id="countrySelectKendo" name="Id">
            <option disabled selected>Select language</option>
            @foreach (var item in ResourceEditor.Managers.XmlManager.GetLanguages())
            {
                <option value="@item.Id">@item.Value</option>
            }
        </select>
    </p>
    <div id="example">
        <div id="grid">

        </div>
    </div>
</div>